<UserControl x:Name="userControl" x:Class="Abide.Wpf.Modules.Editors.Halo2.ValueEditors.Vector3ValueEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid MinWidth="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="i:" Margin="6,0,6,0" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Text="{Binding ElementName=userControl, Path=I, Mode=TwoWay}"/>
        <TextBlock Grid.Column="2" Text="j:" Margin="6,0,6,0" VerticalAlignment="Center"/>
        <TextBox Grid.Column="3" Text="{Binding ElementName=userControl, Path=J, Mode=TwoWay}"/>
        <TextBlock Grid.Column="4" Text="k:" Margin="6,0,6,0" VerticalAlignment="Center"/>
        <TextBox Grid.Column="5" Text="{Binding ElementName=userControl, Path=K, Mode=TwoWay}"/>
    </Grid>
</UserControl>
